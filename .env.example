# Supabase connection for the contact form (src/pages/Contact.tsx).
# These are optional — the app falls back to the project's public credentials
# baked into src/integrations/supabase/safeClient.ts. Set them to point the
# form at a different Supabase project for local development.
#
# Both values are public: the publishable (anon) key is safe to expose in the
# client bundle because every table is protected by row-level security.
VITE_SUPABASE_URL="https://buacamcuqxtizcnovtrq.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_pLAC3bcWM2Jo-ejNBS2wrQ_pcGMClMB"
